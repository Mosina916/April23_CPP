#include <iostream>
using namespace std;

int main(){
	// we should never point a pointer to garbage
	// instead point it to NULL if it is the case
	int a=80;
	// int *ptr=&a;
	// int *ptr=NULL;
	// int *ptr;
	// ptr=NULL;
	cout<<ptr<<endl;
		// int *ptr;
	cout<<2*(*ptr)<<endl;


	
	return 0;
}